server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/betca
    username: postgres
    password: postgres

miw:
  jwt:
    secret: secret to dev

logging:
  level:
    org:
      springframework:
        security: DEBUG